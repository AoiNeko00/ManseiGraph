"""프롬프트(prompt) 템플릿 모듈.

1차 추출, 2차 Cross-check, 동명이인 감지에 사용하는 프롬프트를 정의한다.
"""

# === 1차 추출(Pass 1) 프롬프트 ===
EXTRACTION_PROMPT = """다음은 한국 독립운동(1894-1945) 관련 위키피디아 문서입니다. 이 텍스트에서 엔티티(entity)와 관계(relationship)를 추출해주세요.

## 추출 규칙

### 1. 엔티티 타입
person(인물), organization(단체), event(사건), location(장소), concept(개념/사상)

### 2. 관계 타입
- 행위: member_of(소속), participated_in(참여), founded(설립), led(지도)
- 구조: allied_with(연대), opposed(대립), collaborated_with(협력)
- 영향: influenced(영향), mentor_of(사제관계), succeeded_by(후임)
- 조직 변천: evolved_into(발전), merged_with(통합), split_from(분리)
- 공간/일반: located_in(위치), related_to(관련)

### 3. 이름 통일 및 이명(호) 처리
- 각 엔티티의 이름은 정식 한국어 명칭으로 통일하세요.
  - 정식 명칭 기준: "대한민국 임시정부"(O) vs "임정"(X), "한국광복군"(O) vs "광복군"(X)
- 인물의 호(號)·이명은 별도 엔티티로 분리하지 말고, description에 첫 언급 시 병기하세요.
  - 김구(백범), 안창호(도산), 신채호(단재), 김원봉(약산), 김좌진(백야)
- 텍스트에서 호나 별명으로만 등장해도 정식 이름으로 매핑하세요.

### 4. 동명이인(homonym) 분리
- 같은 이름이라도 활동 시기, 역할, 소속이 다르면 별도 엔티티로 분리하세요.
- 특히 주의할 쌍:
  - 김구(백범, 임시정부 주석) vs 김규식(우사, 외교)
  - 이승만(외교독립론) vs 이동휘(사회주의) vs 이회영(아나키즘) — "이"씨 독립운동가 다수
  - 김좌진(청산리) vs 김원봉(의열단) — 무장투쟁 계열이나 노선 상이
- ID에 역할/시기를 접미사로 붙이세요 (예: kim_kyusik_diplomat)
- description에 활동 시기와 핵심 역할을 반드시 명시하세요.

### 5. 대명사 해소
- "그", "그녀", "장군", "주석" 등 모호한 대명사는 반드시 실명으로 치환하여 추출하세요.
- 직책으로만 언급된 경우("임시정부 주석" → 김구, "초대 대통령" → 이승만) 실명으로 매핑하세요.

### 6. 이념 노선 구분
- 외교독립론(이승만), 무장투쟁론(김원봉, 김좌진), 실력양성론(안창호), 사회주의(이동휘) 등 노선 차이를 description과 관계 설명에 반영하세요.
- 노선 간 협력·갈등·전환도 관계로 추출하세요.

### 7. 조직 변천 계보
- 독립운동 단체의 변천을 evolved_into, merged_with, split_from 관계로 추출하세요.
  - 의열단 → 조선민족혁명당 → 조선의용대 → (일부) 한국광복군 합류
  - 신민회 → 서간도·북간도 독립군 기지 → 북로군정서·서로군정서
  - 대한광복단 → 대한정의단 → 군정부 → 북로군정서

### 8. 식민지 맥락
- 조선총독부, 동양척식주식회사 등 일제 식민 기관도 organization 타입으로 추출하고, description에 억압적 역할을 명시하세요.

### 9. 관계 강도(relationship_strength)
- 관계의 strength 필드를 1~10으로 평가하세요:
  - 직접 행위(의거 수행, 조직 창설, 전투 지휘): 9~10
  - 사상적 영향/계승/사제관계: 7~8
  - 같은 조직 소속/동일 사건 참여: 5~6
  - 간접 관련: 3~4

### 10. 주요 인물 필수 추출
다음 인물이 텍스트에 등장하면 반드시 엔티티로 추출하고 관련 관계를 모두 포함하세요:
김구, 이승만, 안창호, 여운형, 김규식, 신채호, 이동휘, 김원봉, 조소앙, 함태영, 이시영, 홍범도, 김좌진, 박은식, 서재필, 손병희, 유관순, 안중근, 윤봉길, 이봉창, 나석주, 남자현, 윤치호, 한용운, 이광수, 이회영, 지청천

### 11. 직책과 역할
description에 대통령, 주석, 부주석, 의장, 총사령관 등의 직책이 있으면 반드시 기재하세요.

## 출력 형식 (반드시 아래 JSON만 출력하세요. 다른 텍스트는 절대 포함하지 마세요)
```json
{{
  "entities": [
    {{"id": "엔티티_고유ID", "name": "표시이름", "type": "person|organization|event|location|concept", "description": "한줄 설명 (호/이명, 직책/역할/활동시기 포함)"}}
  ],
  "relationships": [
    {{"source": "소스_엔티티_ID", "target": "타겟_엔티티_ID", "type": "관계타입", "description": "관계 설명", "strength": 1-10}}
  ]
}}
```

## 예시

입력 텍스트: "의열단은 1919년 11월 김원봉을 단장으로 만주 지린에서 창립된 무장 독립운동 단체이다. 신채호가 작성한 《조선혁명선언》을 행동강령으로 삼았다."

출력:
```json
{{
  "entities": [
    {{"id": "의열단", "name": "의열단", "type": "organization", "description": "1919년 11월 만주 지린에서 창립된 무장 독립운동 단체로, 일제 식민 기관에 대한 직접적 무력 투쟁을 전개했다"}},
    {{"id": "김원봉", "name": "김원봉", "type": "person", "description": "김원봉(약산)은 의열단의 창립자이자 단장으로, 무장 독립운동을 주도한 혁명가"}},
    {{"id": "신채호", "name": "신채호", "type": "person", "description": "신채호(단재)는 역사학자이자 독립운동가로, 의열단의 행동강령인 《조선혁명선언》을 작성했다"}}
  ],
  "relationships": [
    {{"source": "김원봉", "target": "의열단", "type": "founded", "description": "김원봉은 1919년 의열단을 창립하고 단장을 맡았다", "strength": 10}},
    {{"source": "신채호", "target": "의열단", "type": "influenced", "description": "신채호는 의열단의 행동강령인 《조선혁명선언》을 작성했다", "strength": 8}}
  ]
}}
```

## 문서 내용
{text}
"""

# === 2차 추출(Pass 2) — Multi-pass Cross-check 프롬프트 ===
CROSSCHECK_PROMPT = """다음은 한국 독립운동(1894-1945) 관련 위키피디아 문서입니다.
아래에 이미 추출된 엔티티 목록이 있습니다. 이 목록을 참조하여, 텍스트에서 **누락된 관계(relationship)**를 추가로 찾아주세요.

## 이미 추출된 주요 엔티티 목록
{node_list}

## 규칙

### 1. 직간접 언급 포착
- 위 엔티티 목록에 있는 인물/단체/사건이 이 텍스트에 직간접적으로 언급되면, 해당 관계를 반드시 추출하세요.
- 직접 이름이 나오지 않더라도 다음 경우를 포착하세요:
  - 직책으로 유추: "임시정부 주석" → 김구, "초대 대통령" → 이승만
  - 호(號)로 유추: "백범" → 김구, "도산" → 안창호, "단재" → 신채호, "약산" → 김원봉
  - 대명사 해소: "그", "장군", "단장" 등 → 문맥에서 실명 확인

### 2. 집중 탐색 관계 유형
- 같은 단체에 소속된 인물 간의 관계 (collaborated_with, allied_with)
- 정치적 계승/후임 관계 (succeeded_by, preceded_by)
- 사제/멘토 관계 (mentor_of, influenced)
- 대립/갈등 관계 (opposed, rivaled)
- 이념 노선 간 협력/갈등 (외교독립론 vs 무장투쟁론 vs 사회주의)
- 조직 변천 (evolved_into, merged_with, split_from)

### 3. 관계 강도
- strength 필드를 1~10으로 평가하세요 (직접행위=9~10, 사상적영향=7~8, 동일조직=5~6, 간접=3~4)

### 4. 중복 방지
- 1차 추출에서 이미 잡힌 관계는 중복 출력하지 마세요. 새로 발견한 것만 출력하세요.

### 5. 새 엔티티
- 새로운 엔티티도 발견되면 추가하세요. 정식 명칭을 사용하고 호/이명은 description에 병기하세요.

## 출력 형식 (반드시 아래 JSON만 출력하세요)
```json
{{
  "entities": [
    {{"id": "엔티티_고유ID", "name": "표시이름", "type": "person|organization|event|location|concept", "description": "한줄 설명 (호/이명, 직책/역할 포함)"}}
  ],
  "relationships": [
    {{"source": "소스_엔티티_ID", "target": "타겟_엔티티_ID", "type": "관계타입", "description": "관계 설명", "strength": 1-10}}
  ]
}}
```

## 문서 내용
{text}
"""

# === 동명이인(homonym) 감지 프롬프트 ===
HOMONYM_PROMPT = """다음은 여러 문서에서 추출된 동일 이름의 엔티티들입니다. 이들이 동일 인물인지, 동명이인인지 판별해주세요.

## 이름: {name}
## 각 문서에서의 설명:
{descriptions}

## 판별 규칙
1. **동명이인 판정 기준** — 다음 중 2개 이상 해당하면 동명이인으로 판별하세요:
   - 활동 시기가 겹치지 않음
   - 소속 단체가 근본적으로 다름 (예: 임시정부 vs 조선총독부)
   - 활동 영역이 근본적으로 다름 (예: 외교 vs 무장투쟁 vs 교육 vs 종교)
2. **동일 인물 판정 기준** — 같은 인물이 여러 역할을 한 경우(예: 외교관이자 교육자, 문인이자 독립운동가)는 동일 인물로 판별하세요. description에 모든 역할을 통합 기재하세요.
3. **특히 주의할 사례:**
   - 김규식: 임시정부 외교 활동과 교육 활동은 동일 인물
   - 이승만: 외교 활동과 정치 활동은 동일 인물
   - 같은 성(姓)의 다른 인물을 혼동하지 마세요 (김구 vs 김규식, 이승만 vs 이동휘)
4. 판별 결과를 JSON으로 출력하세요.

## 출력 형식
```json
{{
  "is_homonym": true/false,
  "reasoning": "판별 근거 설명 (50자 이내)",
  "persons": [
    {{"id_suffix": "역할키워드", "name": "표시이름", "description": "통합 설명 (호/이명, 직책, 활동시기 포함)", "period": "활동시기", "role": "핵심역할"}}
  ]
}}
```
위에서 is_homonym이 false이면 persons에 하나만 넣으세요.
"""
