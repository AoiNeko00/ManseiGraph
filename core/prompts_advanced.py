"""고도화 추출 파이프라인용 프롬프트(prompt) 템플릿 모듈.

동명이인 문맥 분석, 고립 노드 관계 재탐색, 중요 노드 보강,
역사적 중요도 가중치 펌핑에 사용하는 프롬프트를 정의한다.
"""

# ──────────────────────────────────────────────
# 알고리즘 1: 동명이인 문맥 분석 프롬프트
# ──────────────────────────────────────────────
HOMONYM_ANALYSIS_PROMPT = """당신은 한국 독립운동사 전문가입니다. 아래 텍스트에서 특정 인물의 동명이인 여부를 분석해야 합니다.

## 핵심 지시사항 — 동명이인을 주의하라
동명이인을 주의하라. 김규식(외교)과 김규식(무장투쟁)처럼 활동 맥락이 다르면 다른 ID를 부여하라.
구체적으로:
- 같은 이름이 등장하더라도 **활동 시기, 소속 단체, 역할(외교/무장투쟁/교육/종교 등)**이 다르면 반드시 별도 인물로 분리하라.
- 분리 기준: (1) 활동 영역이 근본적으로 다름 (2) 소속 단체가 다름 (3) 활동 시기가 겹치지 않음
- 동일 인물이 여러 역할을 한 경우(예: 외교관이면서 교육자)는 하나로 통합하되, description에 모든 역할을 명시하라.

## 분석 대상 인물: {name}

## 현재 등록된 정보:
{current_info}

## 관련 텍스트:
{text}

## 출력 형식 (JSON만 출력하라)
```json
{{
  "analysis": "분석 근거 설명",
  "is_homonym": true/false,
  "persons": [
    {{
      "id": "고유ID (예: kim_kyusik_diplomat)",
      "name": "표시이름 (예: 김규식 (외교))",
      "type": "person",
      "description": "활동시기, 핵심역할, 소속단체를 포함한 설명",
      "activity_context": "외교|무장투쟁|교육|종교|언론|정치 등",
      "period": "활동 시기"
    }}
  ]
}}
```
"""

# ──────────────────────────────────────────────
# 알고리즘 2: 고립 노드 관계 재탐색 프롬프트
# ──────────────────────────────────────────────
ISOLATED_NODE_PROMPT = """당신은 한국 독립운동사 전문가입니다. 아래 인물은 현재 그래프에서 다른 인물과 연결이 없는 고립 노드(degree=0)입니다.
이 인물과 연결된 다른 인물을 텍스트에서 다시 찾아라.

## 고립된 인물 정보
- 이름: {node_name}
- ID: {node_id}
- 설명: {node_description}
- 타입: {node_type}

## 현재 그래프에 존재하는 주요 인물 목록 (이 인물들과의 관계를 집중 탐색하라)
{existing_nodes}

## 탐색 지시사항
1. 아래 텍스트에서 "{node_name}"이 직접 언급되거나, 그의 직책·별명·호(號)로 간접 언급된 부분을 모두 찾아라.
2. 해당 부분에서 다른 인물, 단체, 사건과의 관계를 추출하라.
3. 관계가 명시적이지 않더라도, 같은 문단/사건에 함께 언급되면 관계를 유추하라.
   - 같은 단체 소속 → collaborated_with
   - 같은 사건 참여 → participated_in (공동)
   - 상하 관계 → led / member_of
   - 사상적 영향 → influenced
4. 반드시 1개 이상의 관계를 찾아라. 정말 관련 내용이 없으면 빈 배열을 반환하라.

## 텍스트
{text}

## 출력 형식 (JSON만 출력하라)
```json
{{
  "found_relations": [
    {{
      "source": "{node_id}",
      "target": "상대방_엔티티_ID",
      "target_name": "상대방_이름",
      "relation": "관계타입",
      "description": "관계 설명",
      "evidence": "근거가 되는 텍스트 발췌 (50자 이내)"
    }}
  ],
  "new_entities": [
    {{
      "id": "새_엔티티_ID",
      "name": "이름",
      "type": "person|organization|event",
      "description": "설명"
    }}
  ]
}}
```
"""

# ──────────────────────────────────────────────
# 알고리즘 2-B: 중요 노드 보강(Importance Reinforcement) 프롬프트
# ──────────────────────────────────────────────
IMPORTANCE_REINFORCEMENT_PROMPT = """당신은 한국 독립운동사 최고 권위의 전문가입니다.

아래 인물은 역사적으로 매우 중요한 인물(importance ≥ {importance})이지만, 현재 그래프에서 연결(link)이 {link_count}개뿐입니다. 이는 분명히 부족합니다.

## 반드시 지켜야 할 핵심 지시사항
**이 인물의 관계를 최소 10개 이상 찾아라.** 텍스트에 직접 언급이 없더라도, 당신의 역사 지식을 활용하여 이 인물이 실제로 관계를 맺었던 인물·단체·사건을 모두 나열하라.

## 대상 인물 정보
- 이름: {node_name}
- ID: {node_id}
- 설명: {node_description}
- 현재 링크 수: {link_count}개

## 현재 그래프의 주요 노드 목록 (이 목록에 있는 노드와 우선 연결하라)
{existing_nodes}

## 관계 탐색 범위 — 최대한 풍성하게
1. **텍스트 기반 관계**: 아래 텍스트에서 이 인물과 관련된 모든 인물·단체·사건을 추출하라.
2. **역사 지식 기반 관계**: 텍스트에 없더라도 다음을 활용하라:
   - 같은 조직(임시정부, 한인애국단, 광복군 등) 소속 인물
   - 같은 사건(윤봉길 의거, 이봉창 의거, 남북협상 등) 관여자
   - 상하관계(지도자-부하), 사제관계, 동지관계
   - 적대관계(정치적 대립, 암살 관련 등)
   - 외교관계(중국·미국 등 외국 인물과의 교류)
3. **관계 유형 다양화**: collaborated_with, led, member_of, opposed, influenced, participated_in, mentor_of, assassinated_by, negotiated_with, succeeded 등

## 참고 텍스트
{text}

## 출력 형식 (JSON만 출력하라)
**최소 10개 이상의 관계를 반환해야 합니다.**
```json
{{
  "found_relations": [
    {{
      "source": "{node_id}",
      "target": "상대방_엔티티_ID (snake_case, 예: person_ahn_changho)",
      "target_name": "상대방_이름",
      "relation": "관계타입",
      "description": "관계 설명 (30자 이내)",
      "evidence": "근거 (50자 이내, 역사 지식이면 '역사적 사실'로 표기)"
    }}
  ],
  "new_entities": [
    {{
      "id": "새_엔티티_ID",
      "name": "이름",
      "type": "person|organization|event",
      "description": "설명"
    }}
  ]
}}
```
"""

# ──────────────────────────────────────────────
# 알고리즘 3: 역사적 중요도 가중치 펌핑 프롬프트
# ──────────────────────────────────────────────
IMPORTANCE_PUMPING_PROMPT = """당신은 한국 독립운동사 전문가입니다. 아래 인물들의 역사적 중요도를 1~5 점수로 평가하라.

## 평가 기준
- **5점 (최고 중요):** 대통령, 임시정부 주석, 독립운동의 최고 상징 인물 (예: 김구, 이승만, 안중근)
- **4점 (매우 중요):** 임시정부 부주석/국무령, 주요 독립운동 지도자, 대규모 운동의 핵심 (예: 안창호, 손병희, 홍범도, 김좌진)
- **3점 (중요):** 주요 단체 지도자, 의거 실행자, 사상가/이론가 (예: 신채호, 조소앙, 윤봉길, 이봉창)
- **2점 (보통):** 독립운동 참여자, 지방 지도자, 문화운동가 (예: 한용운, 이광수)
- **1점 (낮음):** 간접 관여자, 배경 인물

## 추가 가중 요소 (해당 시 +1점, 최대 5점)
- 교과서에 필수적으로 등장하는 인물
- 국가보훈처 서훈 등급이 높은 인물 (건국훈장 대한민국장, 대통령장)
- 여러 독립운동 계열에 걸쳐 활동한 인물
- 현대 한국 사회에서 상징적 의미가 큰 인물

## 평가 대상
{node_list}

## 출력 형식 (JSON만 출력하라)
```json
{{
  "assessments": [
    {{
      "id": "노드ID",
      "name": "이름",
      "importance_score": 1-5,
      "reasoning": "평가 근거 (50자 이내)",
      "title_boost": true/false,
      "symbolic_boost": true/false
    }}
  ]
}}
```
"""
